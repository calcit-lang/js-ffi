{
  "name": "@calcit/js-ffi",
  "private": true,
  "version": "0.1.13",
  "packageManager": "yarn@4.12.0",
  "dependencies": {
    "@calcit/procs": "0.13.77"
  },
  "devDependencies": {
    "playwright": "1.62.1",
    "vite": "^8.0.12"
  },
  "scripts": {
    "check": "calcit calcit.cirru --check-only",
    "check:quality": "calcit calcit.cirru analyze quality --baseline config/calcit-quality.cirru --format json",
    "audit:unsafe": "calcit calcit.cirru analyze weak-types --only unsafe-coerce --intent explicit-unsafe --format json",
    "check:node": "calcit --entry node calcit.cirru --check-only",
    "check:browser": "calcit --entry browser calcit.cirru --check-only",
    "compile:node": "node scripts/check-api.mjs --compile node",
    "run:node": "yarn compile:node && node node.mjs",
    "test:contract:node": "yarn compile:node && node node-contract.mjs",
    "test:contract:browser-node": "yarn compile:browser && node browser-node-contract.mjs",
    "compile:browser": "node scripts/check-api.mjs --compile browser",
    "build:browser": "yarn compile:browser && yarn vite build --base=./",
    "serve:browser": "yarn vite --host 127.0.0.1",
    "run:browser": "yarn compile:browser && yarn vite --host 127.0.0.1",
    "format": "calcit calcit.cirru edit format",
    "test": "yarn api:check && yarn check:api && yarn test:tooling && yarn check && yarn check:node && yarn check:browser && yarn check:quality && yarn test:types && yarn test:node && yarn test:browser",
    "test:types": "node tests/type-contract.mjs",
    "test:node": "yarn compile:node && node node.mjs && node node-contract.mjs && node --test tests/node.mjs tests/recipes.mjs",
    "test:browser": "yarn compile:browser && node browser-node-contract.mjs && node tests/run-browser.mjs",
    "check:api": "node scripts/check-api.mjs",
    "api:generate": "node scripts/api-catalog.mjs generate",
    "api:check": "node scripts/api-catalog.mjs check",
    "api:search": "node scripts/api-catalog.mjs search",
    "test:tooling": "node --test tests/api-tooling.mjs"
  },
  "engines": {
    "node": ">=22.12.0"
  }
}
